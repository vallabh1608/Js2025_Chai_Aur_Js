"use strict" //Treats all js code as newer version
let stname = "Vallabh" //String
let rollno = 483 //Number
let isPresent = true //boolean
let state = null // null datatype\ type of this data type is object
let count; // undefined datatype

console.table([typeof stname, typeof rollno, typeof isPresent, typeof state, typeof count])
//Output:-
// ┌─────────┬─────────────┐
// │ (index) │ Values      │
// ├─────────┼─────────────┤
// │stname   │ 'string'    │
// │rollno   │ 'number'    │
// │isPresent│ 'boolean'   │
// │state    │ 'object'    │
// │count    │ 'undefined' │
// └─────────┴─────────────┘